{
  "emeraldwalk.runonsave": {
    "commands": [
        {
            "match": "src\\\\.+\\.js$",
            "cmd": "echo 'Building ${fileBasename}' && node src/index.js -i ${file} -o src/dist/${fileBasename} && cat src/dist/${fileBasename} | npx clipboard"
        }
    ]
}
}
{
  "emeraldwalk.runonsave": {
    "commands": [
        {
            "match": "\\.js$",
            "cmd": "echo 'Building ${fileBasename}' && node index.js && cat dist/letteranimator.js | clipboard"
        }
    ]
}
}
{
  "emeraldwalk.runonsave": {
    "commands": [
        {
            "match": "src\\\\.+\\.js$",
            "cmd": "echo 'Building ${fileBasename}' && node index.js -i ${file} -o dist/${fileBasename} && cat dist/${fileBasename} | npx clipboard"
        }
    ]
}
}